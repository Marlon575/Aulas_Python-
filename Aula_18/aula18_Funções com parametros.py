# Funções com parametros são funções que vão receber os valores por fora.
def soma (a, b): # (a, b) São parametros, eles recebem valores quando função é chamada.EX: soma(10, 10)
    resultado = a + b
    print("resultado da soma:",resultado)
soma(10,10)